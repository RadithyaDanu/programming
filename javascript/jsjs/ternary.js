const nilai = 85;
const ternary = nilai > 80 ? "selamat anda lulus" : "ga lulus";

console.log(ternary);

// ternary ini merupakan bentuk sederhana dari if statement
// jika kondisi bernilai true, nilai pertama akan dicetak
// jika kondisi bernilai false, nilai kedua akan dicetak